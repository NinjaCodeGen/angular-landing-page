export class Banner {
    constructor(
        public name: string,
        public desc: string,
        public url: string,
        public imgUrl: string,
        public imgData: string,
        public order: number
    ) {

    }
}